extends layout

block content
  h1= title
  - var image_names = ['owl1.jpg', 'owl2.jpg', 'owl3.jpg', 'owl4.jpg', 'owl5.jpg'];

  - for (let i = 0; i < 3; i++) 
    - let r = Math.floor(Math.random()*5);
    img(src=`/images/${image_names[r]}`, alt=`${image_names[r]}`, height=300, width=300)