extends layout

block content
  h1= title
  p Welcome to MyWebPage
  button(type="button", onclick="double()" , class="btn btn-dark") i am a button
  div(id="double") Up the amount
  script. 
      let value = 0;
      let power = 1;
      function double() {
          value = value + 1;
          power = power * 2;
          document.getElementById("double").innerHTML = "Two to the power" + value + " is " + power;
      }